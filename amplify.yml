version: 1
applications:
  - appRoot: packages/shared-backend
    backend:
      phases:
        preBuild:
          commands:
            - npm install -g pnpm
            - pnpm install
        build:
          commands:
            - amplify push --yes
      cache:
        paths:
          - node_modules/**/*
          - .turbo
